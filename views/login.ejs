<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: url("https://i.ytimg.com/vi/qK7rjAQT2Qg/maxresdefault.jpg") no-repeat center center/cover;
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background: rgba(0, 0, 0, 0.7);
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 0 25px rgba(255, 69, 0, 0.8);
      text-align: center;
      width: 350px;
    }

    h2 {
      margin-bottom: 20px;
    }

    input[type="text"], input[type="password"] {
      width: 90%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      border-radius: 6px;
      outline: none;
      text-align: center;
    }

    .btn {
      display: inline-block;
      width: 90%;
      padding: 12px;
      margin-top: 15px;
      background: linear-gradient(90deg, #ff4500, #ff6347);
      border: none;
      border-radius: 8px;
      color: white;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      position: relative;
      transition: 0.3s;
    }

    .btn:hover {
      box-shadow: 0 0 25px #ff4500, 0 0 50px #ff6347;
    }

    /* Fire glow animation */
    .btn::after {
      content: "";
      position: absolute;
      top: -5px;
      left: -5px;
      right: -5px;
      bottom: -5px;
      border-radius: 10px;
      background: rgba(255, 69, 0, 0.4);
      filter: blur(10px);
      opacity: 0;
      transition: opacity 0.3s;
    }

    .btn:hover::after {
      opacity: 1;
    }

    a {
      display: block;
      margin-top: 15px;
      color: #ff6347;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Login</h2>
    <form action="/login" method="POST">
      <input type="text" name="username" placeholder="Username" required><br>
      <input type="password" name="password" placeholder="Password" required><br>
      <button class="btn" type="submit">Login</button>
    </form>

    <a href="/register">Don't have an account? Register</a>
    <a href="/auth/discord">
      <button class="btn" type="button">Login with Discord</button>
    </a>
  </div>
</body>
</html>
