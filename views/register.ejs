<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register | ChunkHost</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen">
  <div class="bg-gray-800 p-10 rounded-2xl shadow-lg w-full max-w-md text-center">
    <h2 class="text-3xl font-bold mb-6 text-white">Register</h2>

    <% if (error) { %>
      <p class="text-red-500 mb-4"><%= error %></p>
    <% } %>

    <form method="POST" action="/register" class="flex flex-col space-y-4">
      <input type="text" name="username" placeholder="Username" required
             class="p-3 rounded-lg bg-gray-700 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-green-500">
      <input type="password" name="password" placeholder="Password" required
             class="p-3 rounded-lg bg-gray-700 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-green-500">
      <button type="submit" 
              class="bg-green-500 hover:bg-green-600 text-black font-bold py-3 rounded-lg transition duration-200">
        Register
      </button>
    </form>

    <p class="mt-6 text-gray-400">
      Already have an account? 
      <a href="/login" class="text-green-400 hover:underline">Login</a>
    </p>
  </div>
</body>
</html>
