<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Payment - ChunkHost</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white">
  <div class="max-w-2xl mx-auto p-8 mt-10 bg-gray-800 rounded-2xl shadow-xl">
    <h1 class="text-3xl font-bold text-center mb-6">Complete Your Payment</h1>

    <div class="mb-6 text-center">
      <p class="text-gray-300">Hi <span class="font-semibold"><%= user %></span>, you selected:</p>
      <h2 class="text-2xl font-bold mt-2"><%= planName %></h2>
      <p class="text-xl text-green-400">₹<%= price %> / month</p>
    </div>

    <!-- QR Code Section -->
    <div class="text-center mb-6">
      <h3 class="text-xl font-semibold mb-2">Scan to Pay (UPI)</h3>
      <img 
        src="https://github.com/deadlauncherg/Lapiogaming-skyport/blob/main/WhatsApp%20Image%202025-08-17%20at%2020.33.32_0c465f60.jpg?raw=true" 
        alt="QR Code" 
        class="mx-auto rounded-lg border border-gray-700 shadow-md" 
        width="220"
      >
      <p class="text-sm text-gray-400 mt-2">After payment, enter your details & UTR number below</p>
    </div>

    <!-- Form Section -->
    <form action="/confirm-payment" method="POST" class="space-y-4">
      <input type="hidden" name="planName" value="<%= planName %>">
      <input type="hidden" name="price" value="<%= price %>">
      <input type="hidden" name="user" value="<%= user %>">

      <!-- Panel Email -->
      <div>
        <label class="block text-gray-300 mb-1">Panel Email</label>
        <input type="email" name="panelEmail" required 
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-blue-500 outline-none">
      </div>

      <!-- Panel Password -->
      <div>
        <label class="block text-gray-300 mb-1">Panel Password</label>
        <input type="password" name="panelPassword" required 
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-blue-500 outline-none">
      </div>

      <!-- Contact Email -->
      <div>
        <label class="block text-gray-300 mb-1">Contact Email (for server info)</label>
        <input type="email" name="contactEmail" required 
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-blue-500 outline-none">
      </div>

      <!-- UTR Number -->
      <div>
        <label class="block text-gray-300 mb-1">UTR Number (from payment)</label>
        <input type="text" name="utr" required 
          placeholder="e.g. 234567891234" 
          class="w-full px-4 py-2 rounded-lg bg-gray-700 border border-gray-600 focus:ring-2 focus:ring-green-500 outline-none">
      </div>

      <button type="submit" 
        class="w-full bg-green-600 hover:bg-green-700 px-4 py-3 rounded-lg font-semibold text-white">
        Submit & Verify Payment
      </button>
    </form>

    <div class="mt-6 text-center">
      <a href="/plans" class="text-gray-400 hover:underline">← Back to Plans</a>
    </div>
  </div>
</body>
</html>
